/**
 * @typedef {Object} ImageSchema
 * @property {number} id
 * @property {string} title
 * @property {string} imageUrl
 * @property {string} category
 * @property {number} likes
 */

/**
 * @typedef {'id' | 'title' | 'likes' | 'category'} SortBy
 */

export {}; // 👈 keeps file recognized as a module (optional but recommended)
